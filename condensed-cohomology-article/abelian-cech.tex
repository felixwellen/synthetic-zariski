\begin{definition}
  Let $X$ be a type. Let $\mathcal A$ be a collection of dependent abelian groups $X \to Ab$. Let $q:S \twoheadrightarrow X$. 
  We say $q,\mathcal A$ is \v Cech cover system if:
  \begin{itemize}
    \item Whenever $A:\mathcal A$, 
      so is $\lambda x .A(x)^{S_x}/ A(x)$. 
    \item for all $k>1$ the canonical map $K(\Pi_{x:X} A(x)^{(S_x)^k}, n)\to \Pi_{x:X}(K(A(x),n))^{(S_x)^k}$ is an equivalence. 
  \end{itemize}
\end{definition}  

\begin{lemma}\label{TrivialHasVanishingCohomology}
  If $q:S \twoheadrightarrow X$ is a \v Cech cover system with $\mathcal A$, then $H^n(S,A) = 0$ for all $A:\mathcal A, n >1$. 
\end{lemma}


\begin{lemma}\label{inductive-definition-cohomology-general}
Assume given a type $X$ and for each $x:X$ an Abelian group $A(x)$, with a \v Cech cover $S$. Then for all $n:\N$ we have an exact sequence:
\[H^{n}(X,\lambda x.A(x)^{S(x)}) \to H^{n}(X,L)\to H^{n+1}(X,A)\to 0\]
natural in $A$, where $L(x) = A(x)^{S(x)}/A(x)$.
\end{lemma}

\begin{proof}
From the long exact cohomology sequence we just need to prove that $H^{n+1}(X,\lambda x.A(x)^{S(x)}) = 0$. As $S$ was a \v Cech cover, we have that $\Pi_{x:X}K(A(x)^{S(x)},n+1) = \Pi_{x:X}K(A(x),n+1)^{S(x)}$ so that $H^{n+1}(X,\lambda x.A(x)^{S(x)}) = H^{n+1}(\Sigma_{x:X}S(x),A\circ \pi_S)$ so it is $0$ by \Cref{TrivialHasVanishingCohomology}.
\end{proof}
\rednote{This actually shows that $H^n(X,\lambda x . A(x)^{S(x)} = 0$ for $n\geq 1$. 
  For $n=0$, we don't have this result, but we do have that 
$H^0(X,\lambda x .A(x)^{S(x)}) = \check H^0(X,S,\lambda x .A(x)^{(S(x)}$, which really is all we need. }

%\begin{lemma}
%  Assume given a type $X$ with $Y(x)$ an inhabited type and $A(x)$ an abelian group both depending on $x:X$. 
%  Then $$\check H^0(X,Y, \lambda x.A(x)^Y(x)) = \Pi_{x:X} A_x^{Y_x} = H^0(X, \lambda x. A(x)^Y(x)$$
%\end{lemma}
%\begin{proof}
%  The latter equality follows from the cohomology definition and the observation that $A(x)$ is a set for all $x:X$. 
%  For the first equality, note that 
%  $$\check H^0(X,Y, \lambda x.A(x)^Y(x)) = Ker (\partial_1) = \{\alpha : \Pi_{x:X} (A_x^{Y(x)}^{Y(x)} | 
%\end{proof}


\begin{lemma}\label{long-exact-cech-cohomology-general}
Assume given $X$ and a short exact sequence of abelian groups 
\[0\to A(x)\to B(x)\to C(x)\to 0\]
depending on $x:X$. 
Also assume a surjection $q:S\twoheadrightarrow X$ giving a \v Cech cover for $X,A$. 

Then there is a long exact sequence of \v{C}ech cohomology groups:
\[\check{H}^0(X,S,A) \to\check{H}^0(X,S,B) \to\check{H}^0(X,S,C) \to\check{H}^1(X,S,A) \to\check{H}^1(X,S,B) \to\check{H}^1(X,S,C) \to\cdots \]
Moreover this long exact sequence is natural in the short exact sequence.
\end{lemma}

\begin{proof}
  As $\|\Pi_{x:X}A(x)^{S_x^k} \|_0= 0$ for all $k\geq 1$, 
  the short exact sequence induces a short exact sequence of \v Cech complexes. 
  Then we use the long exact cohomology sequence induced by such a short exact sequence of complexes.
\end{proof}

\begin{lemma}\label{inductive-definition-cech-cohomology-general}
Assume given $X$ a type, $A$ a dependent abelian group and $S$ a \v{C}ech cover. 

Then for all $n:\N$ we have an exact sequence:
\[\check{H}^{n}(X,S,\lambda x.\, A(x)^{S(x)}) \to \check{H}^{n}(X,S,L)\to \check{H}^{n+1}(X,S,A)\to 0\]
natural in $A$ where $L(x)=A(x)^{S(x)}/A(x)$.
\end{lemma}

\begin{proof}
By \Cref{long-exact-cech-cohomology-general}, it is enough to prove $\check{H}^{n+1}(X,S,\lambda x.\, A(x)^{S(x)}) = 0$. 
This is \Cref{cech-coefficient-lemma}.
\end{proof}


\begin{lemma}
  If $X:\CHaus$ and $C:\N \to X \to \Closed$ is a sequence of closed subsets of $X$ such that $\bigcap_{n:\N} C_n = \emptyset$, then there is an $N:\N$ with $\bigcap_{k<N}C_k = \emptyset$. 
\end{lemma}

\begin{lemma}
  If $X:\CHaus$, then there is some $B:\Boole$ and maps $b : B \to (X \times X) \to \Open \times \Closed$ such that blablab
\end{lemma}


\begin{lemma}
  For $A\subseteq X$ a closed subset of a compact Hausdorff space and 
  $f: A \to Y$ a function to a compact Hausdorff space, 
  $graph(f) \subseteq X \times Y$ is closed. 
\end{lemma}
\begin{proof}
  $(x,y)$ lies in $graph(f)$ iff $A(x) \wedge f x = y$, 
  equality in $Y$ is closed and $A$ is closed. 
  As closed propositions are conjunction-closed, $graph(f)$ is closed. 
\end{proof}
